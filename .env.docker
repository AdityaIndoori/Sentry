# Claude Sentry Configuration (Docker)

# LLM Provider: "anthropic" or "bedrock_gateway"
LLM_PROVIDER=bedrock_gateway

# Anthropic API (used when LLM_PROVIDER=anthropic)
ANTHROPIC_API_KEY=sk-ant-your-key-here
ANTHROPIC_MODEL=claude-opus-4-20250514
ANTHROPIC_MAX_TOKENS=16384

# AWS Bedrock Access Gateway (used when LLM_PROVIDER=bedrock_gateway)
BEDROCK_GATEWAY_API_KEY=clinevscodeopenapi
BEDROCK_GATEWAY_BASE_URL=http://Bedroc-Proxy-fYTbaje6iyYG-268555913.us-west-2.elb.amazonaws.com/api/v1
BEDROCK_GATEWAY_MODEL=global.anthropic.claude-opus-4-6-v1
BEDROCK_GATEWAY_MAX_TOKENS=128000

# Security - AUDIT mode means active tools log but don't execute
SENTRY_MODE=AUDIT
PROJECT_ROOT=/app
STOP_FILE_PATH=/app/STOP_SENTRY
MAX_COST_10MIN=5.00
MAX_RETRIES=3
RESTART_COOLDOWN=600

# Watcher — which log files to monitor (comma-separated glob patterns)
# /app/watched/ = the watched/ directory in this repo (always mounted)
# /app/workspace/ = your service (mounted via SERVICE_HOST_PATH)
WATCH_PATHS=/app/watched/*.log,/app/workspace/logs/*.log
POLL_INTERVAL=5

# Service Awareness — where is your service?
# SERVICE_HOST_PATH = absolute path on your HOST machine to the service's source code.
#   This gets mounted into the container at /app/workspace.
#   Docker Compose reads this for the volume mount.
SERVICE_HOST_PATH=F:/TODO_FLASK/test-service
# SERVICE_SOURCE_PATH = container-side path (agents use this to read your code)
SERVICE_SOURCE_PATH=/app/workspace

# Memory
MEMORY_FILE_PATH=/app/data/sentry_memory.json

# Logging
LOG_LEVEL=INFO
ENVIRONMENT=docker
