fastapi==0.115.0
uvicorn[standard]==0.30.0
anthropic==0.39.0
openai>=1.30.0,<2.0.0
aiohttp==3.10.0
pydantic<3.0.0
pydantic-settings>=2.0.0,<3.0.0
python-dotenv==1.0.1
gitpython>=3.1.0

# P1.2 — Persistence. SQLAlchemy 2.0 async + asyncpg (Postgres) /
# aiosqlite (dev + CI). Alembic for migrations. Greenlet is
# required by SQLAlchemy async under the hood.
sqlalchemy[asyncio]>=2.0.29,<3
asyncpg>=0.29,<0.31
aiosqlite>=0.20,<1
alembic>=1.13,<2
greenlet>=3.0

# LangGraph / LangChain for structured orchestration
langgraph>=0.2.0
langchain-core>=0.3.0

# P2.3b — Prometheus metrics (OSS observability). Optional at import
# time: backend/shared/metrics.py no-ops when this isn't installed so
# dev machines without the full production toolchain stay green.
prometheus-client>=0.20,<0.22

# Testing
pytest==8.3.0
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.0
